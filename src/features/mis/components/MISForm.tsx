// MISForm.tsx — REPLACED by the file-upload redesign (decisions.md [P-23]).
// TODO (Builder): implement MISUploadForm here:
//   - <FileDropzone> for Excel/CSV/PDF
//   - Period display (read-only, from useMisForm().data.current_period)
//   - Optional comment textarea
//   - Upload button via useUploadMis() + buildMISFormData()
// See PRD §7.9.2 for the full FormData contract.

export {};
